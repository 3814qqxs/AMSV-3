# AMSV-3 V2 — User Acceptance Testing
**Date:** 2026-05-31
**Tester:**
**Build:** V2 · John 1 default · AMSV
**Environment:** (browser / OS / device)

> **How to use this file**
> Work through each section in sequence. Mark each item `[P]` Pass · `[F]` Fail · `[S]` Skip.
> Add free-form notes in the **Findings** block at the end of each section.
> Carry actionable items into the **V3 Planning Notes** section at the bottom.
>
> **V2 changes under test** are tagged inline: `(1-A)` … `(4-E)`.
> Items without a tag are regression checks from V1.

---

## 1. Welcome Screen `(4-E)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 1.1 | First load (fresh / no localStorage) shows the welcome splash — not the reading pane |F | |
| 1.2 | "Welcome to / Lectio" heading and subtitle appear with staggered animation |P |Upadte to AMSV heading, submititle "Paced reading Armstrong update to ASV"  |
| 1.3 | "Begin with John 1" button is visible and tappable |P | |
| 1.4 | Tapping "Begin with John 1" dismisses the splash and starts playback at John 1 |P | |
| 1.5 | "Choose where to start" button opens an embedded NavSheet |F |Replace both buttons with "Old Testament" and "New Testament".  Should have link directly NavSheet OT / NT |
| 1.6 | Selecting a book + chapter from embedded NavSheet dismisses splash and starts playback at chosen ref |S | |
| 1.7 | Returning to the app after the first visit skips the welcome screen entirely |P | |
| 1.8 | Welcome screen z-index: sits fully above the reading pane (not partially hidden) |S |Unclear what z-index is |

**Findings:**
1.1 Works in Firefox but not in Edge.  
1.5 No action when pressing button, see notes
1.n When slecting John 1, the NavSheet briefly displays then reading pane appears and text begins to scroll

---

## 2. Playback — Tap to Play / Pause `(2-A)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 2.1 | Tapping anywhere on the reading pane toggles play / pause |P |include play / pause symbol in the center of the reading pane briefly after selection |
| 2.2 | A genuine drag (> 5 px movement) does NOT trigger play / pause |P |Change should resume playback at the selected wpm rate |
| 2.3 | Tapping the speed zone does NOT toggle play / pause (has `stopPropagation`) |P | |
| 2.4 | Tapping the Prev / Next arrows at end-of-chapter does NOT toggle play / pause |P |Upate, Prev / Next arrorws should trigger playback when new scripture is loaded |
| 2.5 | Spacebar still toggles play / pause (regression) |P | |
| 2.6 | WPM display reads "paused" when paused, `N wpm` when playing |F | |

**Findings:**
2.6 "paused" only displayed when user reduces speed to 0, should toggle to paused and return to WPM on user input play / pause
2.n When paused low / med / high appear in the bottom right courner, removed
---

## 3. Playback — Word Reveal & Stage

### 3a. Three-Line Stage `(4-B)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 3.1 | Reading stage shows approximately 3 lines of text at a time |P | |
| 3.2 | Stage scrolls upward as new lines arrive (typewriter roll) |P | |
| 3.3 | Bottom of the stage fades into the background (gradient mask visible) |F | |
| 3.4 | Stage does not scroll on the very first word (no jumpiness at start) |P | |
3.3 Grandient mask should be visible on top most line as text.
3.n When arriving at the end of the chapter, the final word should follow normal presentation, and change to white to match prior words
3.nn Some periodic slowness in the text

### 3b. Phrase Mode `(4-A)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 3.5 | Default mode (phraseMode off): words arrive one at a time |S | |
| 3.6 | With phraseMode enabled: words arrive in groups of ≤ 3 |S | |
| 3.7 | Punctuation (`. ? ! , ; :`) breaks a phrase before the 3-word limit |S | |
| 3.8 | Verse markers are always their own beat (not grouped with words) |S | |

### 3c. End-of-Chapter Arrows `(3-C)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 3.9 | After the last word: `← Prev` and `Next →` buttons appear |P | |
| 3.10 | Prev button goes to the previous chapter; Next button goes to the next |P | |
| 3.11 | Tapping Prev / Next does NOT trigger tap-to-pause (stopPropagation working) |P | |
| 3.12 | No "end of chapter" plain text remains — replaced entirely by arrows |P | |

**Findings:**
3.5 Unclear how to turn on / turn off phrase mode, once tested, this should be the default setting, and removing words arriving one at a time
3.11 After prev / next scripture loads, playback should begin automaticaly

---

## 4. Background Layer `(1-A)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 4.1 | Background scrolls once per verse boundary — NOT on every word reveal |P | |
| 4.2 | Multiple words in the same verse: background stays still between words |P | |
| 4.3 | At a verse change: background scrolls exactly once to catch up |P |text should continue to fill background and should move full page only after full screen has been filled with text |
| 4.4 | Background text remains blurred and dimmed (not distracting) — regression |P | |
| 4.5 | Background resets cleanly when a new chapter loads |P | |

**Findings:**
4.3 background text should only move once the entire background pane is filled, and should completely roll up, like swiping to a new page to continue recording the playing text. 

---

## 5. Speed Control

### 5a. Preset Cycle Button `(4-D)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 5.1 | A "Low / Med / High" label button is visible in the bottom-right corner |F |replace WPM with this feature |
| 5.2 | Tapping cycles: Low (150) → Med (250) → High (400) → Low |P | |
| 5.3 | Speed change takes effect immediately on the next word |P | |
| 5.4 | Button is recessed during playback (`chrome-element` class applied) |P | |

### 5b. Fine-tuning (regression)

| # | Test | Result | Notes |
|---|------|--------|-------|
| 5.5 | Scroll / swipe up on tempo zone increases WPM by 50 |S | |
| 5.6 | Scroll / swipe down on tempo zone decreases WPM by 50 |S | |
| 5.7 | WPM floor is 0 ("paused") and cap is 500 |S | |
| 5.8 | Keyboard `↑` / `↓` adjusts WPM by 50 |S | |

**Findings:**
5.1 Only fixable when text is paused, remove for now
5.n replace fine-tuning scroll with High / Med / Low
---

## 6. Chrome Recession `(3-B)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 6.1 | While playing: TopBar stays at full opacity (NOT recessed) |P |Update to display book level NavSheet sub-menu directly at the book and chapter level |
| 6.2 | While playing: verse badge (`v. N`) stays at full opacity (NOT recessed) |P | |
| 6.3 | While playing: watermark (`Lectio · AMSV`) fades to ~20% — regression |P |Remove Lectio |
| 6.4 | While playing: pull-down handle fades to ~20% — regression |P |no need to fade when playing |
| 6.5 | WPM label stays fully visible at all times — regression |P | |
| 6.6 | Pausing restores all recessed elements smoothly |P | |

**Findings:**

---

## 7. Navigation Sheet `(1-B · 1-C · 1-D · 2-D)`

### 7a. Opening & Testament Default `(2-D)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 7.1 | Swiping up on the bottom handle opens the NavSheet |P | |
| 7.2 | When reading a NT book: NavSheet opens to New Testament tab |P | |
| 7.3 | When reading an OT book: NavSheet opens to Old Testament tab |P | |

### 7b. State Reset on Second Open `(1-D)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 7.4 | Navigate to a chapter, close the sheet, re-open: sheet returns to Book Picker view |P | |
| 7.5 | Re-open after navigating: testament tab matches current active scripture |P | |

### 7c. Dismissal from Any View `(1-B · 1-C)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 7.6 | Dragging the pill downward from the Book Picker view dismisses the sheet |P | |
| 7.7 | Dragging the pill downward from the Chapter Picker view also dismisses the sheet |P | |
| 7.8 | Spring close animation plays on dismiss from either view |P | |
| 7.9 | Dismissing without selecting a chapter leaves current chapter unchanged — regression |P | |

### 7d. Book & Chapter Picker (regression)

| # | Test | Result | Notes |
|---|------|--------|-------|
| 7.10 | OT / NT toggle switches the book list |P | |
| 7.11 | Books grouped by section with visible section labels |P | |
| 7.12 | Tapping a book slides to Chapter Picker with horizontal plane transition |P | |
| 7.13 | Correct chapter count shown (John = 21, Psalms = 150) |P | |
| 7.14 | Tapping a chapter: sheet closes, chapter loads, playback starts |P | |
| 7.15 | `← Books` button returns to Book Picker — pinned above chapter grid |P | |

**Findings:**
7.n Remove "close" button, navigation should be pill / book / chapter selection only
---

## 8. History Pane `(2-B · 2-C)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 8.1 | Pulling down from top of reading pane opens History Pane |P | |
| 8.2 | Top drag-handle pill is visible at the top of the pane |P | |
| 8.3 | Swiping UP on the top pill closes the History Pane and returns to reading |F |Draggin up displays book selection |
| 8.4 | Playback resumes after close if it was playing before pull-down |P | |
| 8.5 | Bottom strip pill is visible at the bottom of the pane |F |only one pill visible |
| 8.6 | Swiping UP on the bottom strip opens the NavSheet for browsing |F | |
| 8.7 | No "Close" button remains — interaction is gesture-only |P | |
| 8.8 | No "Change" button remains — replaced by bottom strip |P | |
| 8.9 | Revealed words appear lighter; unrevealed words are dimmed — regression |P | |
| 8.10 | Full chapter text with verse superscripts is readable |P |dimmed words should be closer in color to revealed words |

**Findings:**
8.5 Swiping up only displays NavPane, no direct way to navigate back to reading pane.
8.n The UX between reading pane and history pane is jaring, should either be  aniation or other color queues to connect the two experiences

---

## 9. TopBar & Labels `(3-A)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 9.1 | TopBar shows "AMSV" (not "ASV") |P |Remove from topbar |
| 9.2 | Watermark reads "Lectio · AMSV" (not "Lectio · ASV") |P |Remove Lectio |
| 9.3 | Page title in browser tab reads "Lectio — paced AMSV Bible reader" |P |Remove paced AMSV Bible reader, include placeholder for future text |
| 9.4 | TopBar updates immediately when a new chapter is committed — regression |P |Update, when user clicks on top bar, NavPane for open book is presented |

**Findings:**

---

## 10. Touch Targets — Side Panes `(2-E)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 10.1 | A thin pill indicator is visible on the left edge of the reading card |P |Move to inside the reading pane to match pulldown at top |
| 10.2 | A thin pill indicator is visible on the right edge of the reading card |P |Move to insdie of reading pane |
| 10.3 | Tapping the left edge opens the Cross-references pane |P |Update to include user swipe right |
| 10.4 | Tapping the right edge opens the Commentary pane |P |Update to include user swipe left |
| 10.5 | Edge buttons are recessed during playback |P | |
| 10.6 | Keyboard `←` / `→` still triggers side panes — regression |P |Reverse arrow behavior |

**Findings:**
10.n When navigating using arrows, opposite arrow should hide current pane
10.nn Remove x to close pane, user
---

## 11. Cinematic Cross-Reference Transition `(4-C)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 11.1 | Tapping a cross-reference triggers the transition overlay (not an immediate navigation) |S |No cross reference available, furture version |
| 11.2 | Overlay: current chapter text scrolls rapidly upward |S | |
| 11.3 | Target reference label fades in at center after ~0.65 s |S | |
| 11.4 | After ~1.1 s the target chapter loads and the overlay clears |S | |
| 11.5 | Overlay sits on top of everything (z-index correct) |S | |
| 11.6 | Transition is visually cinematic — text-travel feel, not a cut |S | |

**Findings:**

---

## 12. Long-Book Load `(1-F)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 12.1 | Psalms 23 loads without error |F |Psalms doesn't load, Jeremiah does load |
| 12.2 | Song of Solomon 1 loads without error |F |Does not load |
| 12.3 | 1 Samuel 1 loads without error |P | |
| 12.4 | 1 Corinthians 13 loads without error |P | |
| 12.5 | Verse text and numbers are correct (no off-by-one from import) |F |isalignment until text has loaded and begins playing |

**Findings:**

---

## 13. Performance & Feel `(1-E)`

| # | Test | Result | Notes |
|---|------|--------|-------|
| 13.1 | Word arrival animation is smooth — no jank or dropped frames |F |some slowness |
| 13.2 | NavSheet open / close is smooth on mobile / touch |P | |
| 13.3 | HistoryPane open / close is smooth |F |UX needs improvements, see other notes |
| 13.4 | No layout shift or flicker when chrome recesses during playback |P | |
| 13.5 | Chapter transition animation (blur + y + scale) is smooth |P | |
| 13.6 | No memory creep after reading 5+ chapters in sequence |P | |

**Findings:**

---

## 14. Overall Impressions

**What works well:**

**What feels broken or incomplete:**

**What's confusing or needs explanation:**

**Top 3 priorities for V3:**
1. Reading pane updates
2. History pane navigation
3. Replacing WPM with High / Med / Low
4. Psalms does not load
---

## V3 Planning Notes

> Carry forward specific, actionable findings from above. Each item becomes a candidate for the next `/brainstorm` or `/plan` session.

### Must Fix (regressions / broken experience)

- [1.1 Works in Firefox but not in Edge.  
1.5 No action when pressing button, see notes
 2.6 "paused" only displayed when user reduces speed to 0, should toggle to paused and return to WPM on user input play / pause
2.n When paused low / med / high appear in the bottom right corner, removed
3.5 Unclear how to turn on / turn off phrase mode, once tested, this should be the default setting, and removing words arriving one at a time
3.11 After prev / next scripture loads, playback should begin automatically
4.3 background text should only move once the entire background pane is filled, and should completely roll up, like swiping to a new page to continue recording the playing text. 
5.1 Only fixable when text is paused, remove for now
5.n replace fine-tuning scroll with High / Med / Low
8.5 Swiping up only displays NavPane, no direct way to navigate back to reading pane.
8.n The UX between reading pane and history pane is jaring, should either be  aniation or other color queues to connect the two experiences
10.n When navigating using arrows, opposite arrow should hide current pane
10.nn Remove x to close pane, user] *(populate from findings above)*

### Should Have (meaningful UX gaps)

- [1.n When selecting John 1, the NavSheet briefly displays then reading pane appears and text begins to scroll 

] *(populate from findings above)*

### Nice to Have (polish / delight)

- [7.n Remove "close" button, navigation should be pill / book / chapter selection only ] *(populate from findings above)*

### Architecture / Infrastructure Flags

- Any feature requiring user identity (reading history, bookmarks, shared annotations) still needs a `users` table — flag before designing sign-in.
- Commentary + cross-reference data pipeline (GitHub-based entry) — separate activity, not blocking V3.
- 66-book load verification: if 12.x failures found, run systematic endpoint sweep before planning sprint.

---

*End of UAT document. Findings to be carried into `2026-05-31-v3-development-plan.md`.*
